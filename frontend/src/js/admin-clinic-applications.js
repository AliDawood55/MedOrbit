const AdminClinicApplications=(()=>{let apps=[],selected=null;const $=id=>document.getElementById(id),ar=()=>I18n?.getLang?.()==='ar',esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const name=a=>{const x=a.applicant||{};return ar()?[x.first_name_ar,x.last_name_ar].filter(Boolean).join(' ')||x.email:[x.first_name_en,x.last_name_en].filter(Boolean).join(' ')||x.email};function msg(t,type='error'){$('reviewAlert').className=`alert ${type}`;$('reviewAlert').textContent=t}function render(){ $('reviewLoading').classList.add('hidden');$('reviewEmpty').classList.toggle('hidden',apps.length>0);$('reviewList').innerHTML=apps.map(a=>`<button class="review-item${selected?.id===a.id?' active':''}" data-id="${esc(a.id)}"><strong>${esc(a.name_ar)} / ${esc(a.name_en)}</strong><div>${esc(name(a)||'Applicant')}</div><small>${esc(a.city)} · ${esc(a.status)}</small></button>`).join('');$('reviewList').querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>{selected=apps.find(a=>a.id===b.dataset.id);render();detail()})}function detail(){if(!selected){$('reviewDetail').innerHTML='<div class="empty-state"><h3>اختر طلباً / Select an application</h3></div>';return}const a=selected,pending=a.status==='pending';$('reviewDetail').innerHTML=`<div class="application-hero"><div><h2>${esc(a.name_ar)} / ${esc(a.name_en)}</h2><p>${esc(a.applicant?.email||'')}</p></div><span class="application-status ${esc(a.status)}">${esc(a.status)}</span></div><div class="review-detail-grid"><div class="review-field"><strong>City / المدينة</strong>${esc(a.city)}</div><div class="review-field"><strong>Type / النوع</strong>${esc(a.type)}</div><div class="review-field"><strong>Registration / الترخيص</strong>${esc(a.registration_number)}</div><div class="review-field"><strong>Phone / الهاتف</strong>${esc(a.phone)}</div><div class="review-field full"><strong>Services / الخدمات</strong>${esc((a.services||[]).join(', '))}</div><div class="review-field full"><strong>Address / العنوان</strong>${esc(a.address_ar)}<br>${esc(a.address_en)}</div></div>${pending?'<div class="form-group"><label for="rejectionReason">سبب الرفض / Rejection reason</label><textarea id="rejectionReason"></textarea></div><div class="application-actions"><button id="approveApplication" class="btn btn-primary">Approve / موافقة</button><button id="rejectApplication" class="btn btn-danger">Reject / رفض</button></div>':''}`;$('approveApplication')?.addEventListener('click',approve);$('rejectApplication')?.addEventListener('click',reject)}async function load(){try{$('reviewLoading').classList.remove('hidden');const r=await API.clinicApplications.adminList($('statusFilter').value?{status:$('statusFilter').value}:null);apps=r.data||[];selected=null;render();detail()}catch(e){msg(e.message)}finally{$('reviewLoading').classList.add('hidden')}}async function approve(){if(!confirm('Approve this clinic application?'))return;try{await API.clinicApplications.approve(selected.id);Toast.success('Clinic approved');load()}catch(e){msg(e.message)}}async function reject(){const reason=$('rejectionReason').value.trim();if(!reason){msg('Rejection reason is required');return}try{await API.clinicApplications.reject(selected.id,{rejection_reason:reason});Toast.success('Application rejected');load()}catch(e){msg(e.message)}}async function init(){if(!API.requireAuth('admin-clinic-applications.html'))return;if(await AuthGate.verifySession()!=='valid')return;if(!['admin','super_admin'].includes(AuthGate.getVerifiedUser()?.role)){location.href='dashboard.html';return}$('statusFilter').addEventListener('change',load);window.addEventListener('languageChanged',()=>{render();detail()});load()}return{init}})();
